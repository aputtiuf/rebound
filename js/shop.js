document.addEventListener('DOMContentLoaded',()=>{const cart=[];const cartEl=document.getElementById('cart');function render(){if(!cart.length){cartEl.classList.remove('show');return}cartEl.classList.add('show');cartEl.innerHTML='<h3>Demo Cart</h3>'+cart.map(i=>`<p class="muted">${i.name} — <b>${i.price}</b></p>`).join('')+'<button class="btn" onclick="document.getElementById(\'cart\').classList.remove(\'show\')">Close</button>'}document.querySelectorAll('[data-add]').forEach(b=>b.addEventListener('click',()=>{cart.push({name:b.dataset.add,price:b.dataset.price});render();toast('Added to demo cart')}));});
