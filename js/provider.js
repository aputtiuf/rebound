document.addEventListener('DOMContentLoaded',()=>{const form=document.getElementById('planForm');const preview=document.getElementById('planPreview');renderPlan('#planPreview');form?.addEventListener('submit',e=>{e.preventDefault();const fd=new FormData(form);const plan=Object.fromEntries(fd.entries());savePlan(plan);renderPlan('#planPreview');toast('Treatment plan sent to Client View');});});
